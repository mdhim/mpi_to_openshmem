#include <stdio.h>
#include <stdlib.h>
#include <shmem.h>

int main(int argc, char **argv) {
	printf ("Simple2:hello  *************\n");

	int numPes = atoi ( getenv ("SLURM_JOB_NUM_NODES") );
	printf("Openshmem: num nodes= %d\n", numPes);
	
	// Need a way to get the number of PEs
	// 		
	printf("Openshmem: start_pes()\n");
	start_pes(0);

	printf("Openshmem: After: start_pes()\n");
	//int me = _my_pe ();
  	//int npes = _num_pes ();

  	//printf ("Hello from node %4d of %4d\n", me, npes);

	return (0);
}
